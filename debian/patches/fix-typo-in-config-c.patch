Fix typo writeable -> writable--- a/config.c
+++ b/config.c
@@ -216,7 +216,7 @@
 			}
 		} else {
 			retval = errno;
-			LLDPAD_ERR("%s is not readable and writeable",
+			LLDPAD_ERR("%s is not readable and writable",
 				cfg_file_name);
 		}
 	}
Index: lldpad/config.c
===================================================================
--- lldpad.orig/config.c	2013-07-16 19:24:33.656904388 +0530
+++ lldpad/config.c	2013-07-16 19:24:33.652904389 +0530
@@ -223,7 +223,7 @@
 			}
 		} else {
 			retval = errno;
-			LLDPAD_ERR("%s is not readable and writeable",
+			LLDPAD_ERR("%s is not readable and writable",
 				cfg_file_name);
 		}
 	}
